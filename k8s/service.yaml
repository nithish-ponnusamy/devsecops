apiVersion: v1
kind: Service
metadata:
  name: devsecops-service
  namespace: devsecops
  labels:
    app: devsecops-app
spec:
  type: NodePort
  selector:
    app: devsecops-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
